$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json

name: blue
endpoint_name: iris-endpoint-joshua

model:
  path: ../models
  type: mlflow_model

# Nur Timing-Felder erlaubt (Schema v2) – KEIN path/port
liveness_probe:
  initial_delay: 300      # gib dem Container Zeit, conda/pip zu installieren
  period: 10
  timeout: 5
  failure_threshold: 60

readiness_probe:
  initial_delay: 300
  period: 10
  timeout: 5
  failure_threshold: 60

request_settings:
  request_timeout_ms: 15000
  max_concurrent_requests_per_instance: 1

instance_type: Standard_E2s_v3
instance_count: 1
